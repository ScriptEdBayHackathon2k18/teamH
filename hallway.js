/* global createCanvas, line, mouseIsPressed, fill, mouseX, mouseY, ellipse , angleMode , DEGREES */
function setup()
{
    createCanvas(400, 600);   
    angleMode(DEGREES);
    line(0,1000,0,0);
    line(399,1000,399,0);
}
